# Latitude #198 YOLO/OpenVINO camera-authority runtime.
# Kept separate from base requirements so hosts that do not run this camera path stay lean.
# The filename is historical from the shadow bakeoff; do not infer shadow-only authority from it.
# OpenVINO telemetry must be disabled after install with: opt_in_out --opt_out
openvino==2026.3.0
