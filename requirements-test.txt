# Lean subset of requirements.txt for the CI `test` job.
#
# Why a separate file: full requirements.txt pulls lightgbm, mediapipe,
# ai-edge-litert, scikit-learn, etc. — none of which the pytest suite
# currently exercises — and adds 2-3 minutes to every CI run. This file
# is the minimum set needed for `backend.main:app` to import cleanly so
# tests/ can be collected.
#
# Keep in sync: when CI tests collect or execute a backend path that requires
# an optional dependency, include that dependency here. Missing lazy/runtime
# imports may fail only when the affected test executes, not at collection.

fastapi>=0.135.0
uvicorn[standard]>=0.30.0
websockets>=12.0
aiosqlite>=0.19.0
sqlalchemy[asyncio]>=2.0.0
pydantic-settings>=2.0.0
python-dotenv>=1.0.0
httpx>=0.27.0
aiofiles>=23.0.0
slowapi>=0.1.9
phue2>=0.0.3
fauxmo>=0.8.0
soco>=0.30.0
python-json-logger>=3.0.0
sentry-sdk[fastapi]>=2.18.0
edge-tts>=6.0.0
psutil>=5.9.0
mss>=9.0.0
python-multipart>=0.0.9
pytest>=8.0.0
pytest-asyncio>=0.23.0
numpy>=1.20.0
opencv-python-headless==4.13.0.92
