"""Passive camera decision constants safe for lightweight consumers."""

# Strong-face bar shared by CameraService and TransitLightingService.
# Keep this value stable unless the camera authority policy itself changes.
FACE_TRUST_THRESHOLD = 0.70
