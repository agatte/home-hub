# Desktop-only additions (Anthony's Windows desktop 192.168.86.30).
#
# Install on the desktop with:
#   pip install -r requirements.txt -r requirements-desktop.txt
#
# The Latitude (192.168.86.210, headless Ubuntu) does NOT install this file —
# the server is Qt-free by design. PyQt6 ships ~200 MB of native Qt and isn't
# useful on a kiosk-only box.

# Desktop notifier — frameless toast widget + system tray icon. See
# backend/services/pc_agent/desktop_notifier.py. websockets (>=12) is
# already pulled in by requirements.txt; the .sync.client submodule
# powers the WS subscription off the main Qt thread.
PyQt6>=6.7,<7

# Windows Global System Media Transport Controls playback truth for browser
# Watching detection. Keep the modular projection packages on one exact ABI
# version; the broad [all] extra pulls in unrelated Windows namespaces.
winrt-Windows.Media.Control==3.2.1
winrt-Windows.Foundation==3.2.1
winrt-Windows.Foundation.Collections==3.2.1
