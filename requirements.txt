# Web framework
fastapi>=0.135.0
uvicorn[standard]>=0.30.0
websockets>=12.0

# Device control
phue2>=0.0.3              # Hue bridge control (Python 3.10+)
soco>=0.30.0              # Sonos control via UPnP
fauxmo>=0.8.0             # WeMo emulation for local Alexa voice control

# TTS
edge-tts>=6.0.0           # Free Microsoft TTS (primary)
gtts>=2.5.0               # Google TTS (fallback)

# Database
aiosqlite>=0.19.0
sqlalchemy[asyncio]>=2.0.0

# Config
pydantic-settings>=2.0.0
python-dotenv>=1.0.0

# HTTP
httpx>=0.27.0             # Async HTTP client for ESPN API

# PC activity detection
psutil>=5.9.0             # Process monitoring for activity detection
pyaudio>=0.2.14           # Blue Yeti mic ambient noise detection
mss>=9.0.0                # Fast screen capture for screen sync

# ML
numpy>=1.20.0
scikit-learn>=1.0.0       # K-means for screen sync color clustering
lightgbm>=4.0.0           # Behavioral mode prediction
ai-edge-litert>=1.0.0     # TFLite runtime for YAMNet audio classification
mediapipe>=0.10.0         # Face detection + BlazePose (camera presence)
opencv-python-headless>=4.9  # Camera capture (headless = no GUI deps on kiosk)

# Utilities
aiofiles>=23.0.0          # Async file operations

# Developer tooling
fastmcp>=2.0.0            # MCP server for Claude Code integration
ruff>=0.4.0               # Python linting + formatting

# Testing
pytest>=8.0.0
pytest-asyncio>=0.23.0

# Rate limiting
slowapi>=0.1.9

# Observability
python-json-logger>=3.0.0    # Structured JSON logs for file handler
