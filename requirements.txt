# Pinned to the versions actually running on the Latitude (2026-05-17).
# Global CLAUDE.md mandates `==`; floating `>=` was the source of a
# silent-CVE-update concern flagged in the public-tunnel security audit.
# When bumping a pin, do it deliberately + run tests + redeploy + watch
# the journal for one cycle. Never accept Dependabot/`pip-audit` autobumps
# without that loop.

# Web framework
fastapi==0.135.3
uvicorn[standard]==0.44.0
websockets==16.0
python-multipart==0.0.9   # File/Form parsing for /api/camera/desktop/snapshot/upload
                          # (transitive via fastapi[all], pinned for explicit version control)

# Device control
phue2==0.0.4              # Hue bridge control (Python 3.10+)
soco==0.31.0              # Sonos control via UPnP
fauxmo==0.8.0             # WeMo emulation for local Alexa voice control
                          # NOTE: upstream abandoned Apr 2024. Tracked
                          # in flag queue for replacement / removal.

# TTS
edge-tts==7.2.8           # Free Microsoft TTS (primary)
gtts==2.5.4               # Google TTS (fallback)

# Database
aiosqlite==0.22.1
sqlalchemy[asyncio]==2.0.49

# Config
pydantic-settings==2.13.1
python-dotenv==1.2.2

# HTTP
httpx==0.28.1             # Async HTTP client for ESPN API

# PC activity detection
psutil==7.2.2             # Process monitoring for activity detection
pyaudio==0.2.14           # Blue Yeti mic ambient noise detection
mss==10.1.0               # Fast screen capture for screen sync
screen-brightness-control==0.24.1  # DDC/CI monitor brightness (desktop monitor_brightness agent)
monitorcontrol==4.2.0     # DDC/CI VCP color-preset control (desktop monitor_brightness agent)
tzdata==2026.2            # IANA tz database for ZoneInfo on Windows (system has none)

# ML
numpy==2.4.4
scikit-learn==1.8.0       # K-means (screen sync) + IsotonicRegression (predictor calibration)
lightgbm==4.6.0           # Behavioral mode prediction
ai-edge-litert==2.1.4     # TFLite runtime for YAMNet audio classification
mediapipe==0.10.33        # Face detection + BlazePose (camera presence)
opencv-python-headless==4.13.0.92  # Camera capture (headless = no GUI deps on kiosk)

# Utilities
aiofiles==25.1.0          # Async file operations

# Developer tooling
fastmcp==3.2.3            # MCP server for Claude Code integration
ruff==0.15.10             # Python linting + formatting

# Testing
pytest>=8.0.0             # Test-only; floating is fine here
pytest-asyncio>=0.23.0    # Test-only; floating is fine here

# Rate limiting
slowapi==0.1.9

# Observability
python-json-logger==4.1.0    # Structured JSON logs for file handler
sentry-sdk[fastapi]==2.59.0  # Error reporting + FastAPI request-context auto-tagging
